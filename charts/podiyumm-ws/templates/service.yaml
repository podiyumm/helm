apiVersion: v1
kind: Service
metadata:
  name: {{ include "podiyumm-ws.fullname" . }}
  labels:
    {{- include "podiyumm-ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podiyumm-ws.selectorLabels" . | nindent 4 }}
