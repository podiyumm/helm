apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podiyumm-ws.fullname" . }}-test-connection"
  labels:
    {{- include "podiyumm-ws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "podiyumm-ws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
