apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podiyumm-landing-ui.fullname" . }}-test-connection"
  labels:
    {{- include "podiyumm-landing-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "podiyumm-landing-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
