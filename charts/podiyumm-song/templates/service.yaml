apiVersion: v1
kind: Service
metadata:
  name: {{ include "podiyumm-song.fullname" . }}
  labels:
    {{- include "podiyumm-song.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podiyumm-song.selectorLabels" . | nindent 4 }}
