apiVersion: v1
kind: Service
metadata:
  name: {{ include "podiyumm-bible.fullname" . }}
  labels:
    {{- include "podiyumm-bible.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podiyumm-bible.selectorLabels" . | nindent 4 }}
