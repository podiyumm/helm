apiVersion: v1
kind: Service
metadata:
  name: {{ include "podiyumm-musician-ui.fullname" . }}
  labels:
    {{- include "podiyumm-musician-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podiyumm-musician-ui.selectorLabels" . | nindent 4 }}
